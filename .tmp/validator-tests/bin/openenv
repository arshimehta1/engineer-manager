#!/usr/bin/env bash
set -euo pipefail

printf '%s\n' "$*" >> "${MOCK_LOG_DIR}/openenv-calls.txt"

if [[ "${1:-}" == "validate" ]]; then
  if [[ "${MOCK_OPENENV_VALIDATE:-ok}" == "ok" ]]; then
    printf 'mock validate ok\n'
    exit 0
  fi
  printf 'mock validate failed\n' >&2
  exit 1
fi

exit 0
